const BASE_CATALOG_ENDPOINT = 'admin';

export const CATALOG_ENDPOINTS = {
    budgets: `${BASE_CATALOG_ENDPOINT}/budgets`,
    expenses: `${BASE_CATALOG_ENDPOINT}/expenses`,
    profile: `${BASE_CATALOG_ENDPOINT}/users`,
};
